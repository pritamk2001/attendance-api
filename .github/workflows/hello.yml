name: My First Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello GitHub Actions"
      - run: pwd
      - run: ls -la
      - run: whoami
      - run: uname -a
